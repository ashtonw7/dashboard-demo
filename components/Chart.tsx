'use client'

interface Props {
    chartId: string,
    containerStyle: React.CSSProperties,
}

export default function Chart({chartId, containerStyle}: Props) {
    return(
        <>

        </>
    )
}